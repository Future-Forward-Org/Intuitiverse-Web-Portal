enum ButtonNames {
  START
  CONTINUE
  COMPLETED
}

enum Progress {
  NEW
  INPROGRESS
  COMPLETE
}

type Tasks @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  url: AWSURL
  progressLabel: String
  buttonName: String
  type: String
}
 